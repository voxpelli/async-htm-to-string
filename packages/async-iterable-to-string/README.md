# @voxpelli/async-iterable-to-string

Asyncly concatenates an iterable (sync or async) of strings into a single string.

## API

### `generatorToString(generator)`

Takes an `AsyncIterable<string>` or `Iterable<string>` and returns a `Promise<string>` with the concatenated result.

## License

0BSD
